# cat=basic/enable; type=boolean; label=Enable enableLogging:Log API requests
enableLogging = 0

# cat=basic/enable; type=boolean; label=Activate Demo APIs:Register example APIs and resources
activateDemoApis = 1

# cat=basic/enable; type=boolean; label=Activate Legacy Support:Enable support for sg_rest legacy calls and tokens
activateLegacySupport = 0

# cat=basic/logging; type=boolean; label=Log request headers
logHeaders = 0

# cat=basic/logging; type=boolean; label=Log request body
logBody = 0

# cat=basic/logging; type=boolean; label=Log response body
logResponse = 0

# cat=basic/logging; type=string; label=Comma-separated list of keys to mask in logs
redactKeys = password,token,authorization,secret,access_token,refresh_token,authtoken,bearertoken

# cat=basic/logging; type=int; label=Max body length for logging (0 = unlimited)
logBodyMaxLength = 4096

# cat=basic/cache; type=boolean; label=Enable API caching:Globally enable or disable response caching for the API
enableCaching = 1

# cat=basic/expiration; type=int; label=JWT Access Token TTL:Time in seconds until a JWT Access Token expires (falls back to tokenExpirationTime)
jwtAccessTokenTtlSeconds = 86400

# cat=basic/expiration; type=int; label=Opaque Access Token TTL:Time in seconds until an opaque (database) access token expires
opaqueAccessTokenTtlSeconds = 3600

# cat=basic/expiration; type=int; label=Refresh Token TTL:Time in seconds until a refresh token expires (default 30 days)
refreshTokenTtlSeconds = 2592000

# cat=basic/path; type=string; label=The path prefix for the API (default: /api/)
apiPathPrefix = /api/

# cat=basic/tenant; type=string; label=The source for the tenant ID when using the site strategy (identifier|baseHost|rootPageId)
siteTenantIdSource = identifier

# cat=basic/tenant; type=int; label=The HTTP status code to return when the tenant is missing (404|400)
onMissingTenant = 404

# cat=basic/response; type=boolean; label=Enable response envelope:Wrap success responses in a "data" property
responseEnvelope = 0

# cat=basic/rate-limit; type=boolean; label=Enable rate limiting:Apply a fixed window rate limit to API requests
rateLimitEnabled = 1

# cat=basic/rate-limit; type=int; label=Default rate limit:Requests per window
rateLimitDefaultLimit = 60

# cat=basic/rate-limit; type=int; label=Rate limit window in seconds
rateLimitWindowSeconds = 60

# cat=basic/rate-limit; type=int; label=Default burst size:Additional requests allowed per window
rateLimitDefaultBurst = 0

# cat=basic/security; type=int; label=Backend user UID for Auto-CRUD resource writes:Use this BE user for create/update/delete in Auto-CRUD resources. Leave empty or 0 to keep the admin bypass.
apiResourceWriteBackendUserId = 0
